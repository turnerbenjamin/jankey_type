#ifndef CONFIG_H
#define CONFIG_H

#define WORDS_PER_TEST 50
#define MAX_CHARS_PER_LINE 66
#define MAX_TEST_WIN_ROWS 3

#define MAX_N(a, b) ((a) > (b) ? (a) : (b))
#define MIN_N(a, b) ((a) > (b) ? (b) : (a))

#endif
